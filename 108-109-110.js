108;

function convertToInteger(str) {
    const a = parseInt(str)
    return a
}

convertToInteger("56");

109;

function convertToInteger(str) {
    const a = parseInt(str, 2)
    return a
}

convertToInteger("10011");